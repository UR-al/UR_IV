# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual Environment
venv/
env/

# IDE
.vscode/
.idea/
.pytest_cache/
.mypy_cache/

# Claude
.claude/

# Caches
image_cache/
web_cache/
backend_ui_cache/

# User-specific config (절대 경로, 개인 설정 포함)
# 이제 user_data/ 폴더로 통합 (config.py가 import 시 루트→user_data 자동 마이그레이션)
user_data/
# 세션/검색 캐시와 진단 로그 — 설정과 물리적으로 분리
cache/
logs/
# QWebEngine 저장소/캐시 — 고정 경로(localStorage 영속용). 커밋 금지
web_profile/
# 하위 호환: 옛 위치에 남아있을 수 있는 루트 파일들도 계속 무시
prompt_settings.json
prompt_presets.json
character_presets.json
favorite_tags.json
favorites.json
event_gen_settings.json
search_tab_settings.json
yolo_model_config.json
prompt_history.json

# Runtime / user state (런타임에 자동 생성·갱신)
config/gen_stats.json
config/queue_state.json
config/cond_rules.json
config/ui_prefs.json
config/chat_threads.json
config/tab_defaults.json
config/prompt_order.json
config/profiles/
config/gallery_last_folder.txt
config/last_crash.log
config/last_search_results.json
config/last_full_results.json
config/last_deck.json
Editor_models/yolo_config.json
# User outputs
save/
ex/
generated_images/

# Large data files
*.parquet
!tags_db/**/*.parquet
*.sqlite

# Model files
*.pt
*.pth
*.onnx
*.safetensors

# Generated output
generated_images/
ex/

# Logs & backups
*.log
*.log.*
*.bak

# Archives
*.zip

# Raw Danbooru wiki/export workspaces (runtime data lives in tags_db/)
danbooru wiki/
danbooru_wiki/

# Windows
nul

# Legacy generated sources. Runtime-ready replacements live under tags_db/characters/.
/danbooru_character.py
/danbooru_character_description_full.jsonl

# 개인 디버그/스크래치 (참조 안 되는 일회성 파일들을 한데 모음)
/scratch/
Thumbs.db
Desktop.ini

# Temporary
*.tmp
*.swp
config/session_backup.json
config/char_global_prefs.json
config/prompt_settings.json
config/backend_runtime.json
config/forge_model_paths.json
config/app_update.json
config/model_downloads/
config/comfy_workflow_controls.json
config/comfy_quality_preset.json
config/comfy_compatibility_baselines.json
config/automation/
config/state/
# 사용자가 작성한 대화/보조/스키마 지침 프리셋과 글로벌 태그 가중치 — 개인 프롬프트가
# 공개 원격으로 올라가지 않게 (백업은 settings_backup.CONFIG_EXPORT_FILES 가 담당)
config/instruction_presets.json
config/global_weights.json

# 디자인 캔버스 시드 산출물 — .dc.html 소스에서 다시 만들 수 있고 2.5MB 다
design/**/*.html
!design/**/*.dc.html

# 에이전트별 로컬 설정 — 저장소에 넣지 않는다 (.claude/ 와 같은 기준)
.agents/
.codex/
