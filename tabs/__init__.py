# tabs/__init__.py
"""Tab 모듈 — 숨은 레거시 PyQt 탭(Vue 가 화면을 맡고, 백엔드 로직만 참조된다).

재수출하지 않는다: ui/generator_ui_setup.py 등 사용처는 ``from tabs.<모듈> import ...``
로 직접 가져온다. 이 파일을 지우지는 말 것 — tests/test_legacy_ui_retirement.py 가 읽는다.
"""
